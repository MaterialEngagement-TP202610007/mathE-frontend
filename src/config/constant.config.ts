export enum ROUTING {
  HOME = "/",
}

export enum ENDPOINT_SERVER {
  HOME_CONTENT = "/home-content",
}

export enum BREAKPOINTS {
  MOBILE = 475,
  TABLET = 744,
  LAPTOP = 1232,
  DESKTOP = 1440,
}